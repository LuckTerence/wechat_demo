# uni-app frontend should call a server-side proxy endpoint.
# Do NOT put provider API keys in VITE_* variables.

# Example:
# VITE_AI_REPLY_URL=http://127.0.0.1:8787/api/ai-reply
# For phone testing on same LAN:
# VITE_AI_REPLY_URL=http://<your-pc-lan-ip>:8787/api/ai-reply

VITE_AI_REPLY_URL=

# Backend env (used by `npm run dev:api` only).
# Keep these on server and never expose to client code.
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_MODEL=qwen-plus
OPENAI_FALLBACK_MODEL=qwen-turbo
OPENAI_API_KEY=
PORT=8787
