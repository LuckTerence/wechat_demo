# Keep real keys in .env.local (never commit them)
# OpenAI-compatible provider (recommended if you use third-party OpenAI format APIs)
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_API_KEY=your_openai_or_compatible_key_here

# Gemini provider (fallback when OPENAI_API_KEY is not configured)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional model overrides
# GEMINI_MODEL=gemini-2.0-flash
